import React, {Component} from 'react';

class Header extends Component {
    render() {
        return (
            <div className="text-center badge-danger m-2 h-100">
                CRUD with spring boot & react
            </div>
        );
    }
}

export default Header;
